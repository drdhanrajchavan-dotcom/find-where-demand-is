import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "LaunchAgent — Find where demand is. Launch there.",
  description:
    "LaunchAgent reverses the marketing pipeline. 12 Gemini 3.5 Flash agents fan out across Reddit, Hacker News, GitHub, Dev.to, and Stack Overflow to find conversations where people are already asking for your product, then draft platform-perfect posts in their own vocabulary.",
};

export default function RootLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
